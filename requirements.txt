# Dependency spec for Streamlit Community Cloud / Hugging Face Spaces.
#
# `.[dashboard]` installs the Zephyrus package from ./pyproject.toml (core runtime deps:
# pulp/CBC, pandas, numpy, pyarrow, httpx, pydantic, typer …) plus its dashboard extra.
# The dashboard libraries are also pinned explicitly so Cloud always reinstalls them
# (its env cache is keyed on this file). Auto-refresh uses Streamlit's built-in
# components.html — no third-party refresh package required.
.[dashboard]
streamlit>=1.33
plotly>=5.20
