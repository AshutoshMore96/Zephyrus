# Notebooks

Exploratory analysis only — production logic lives in `src/zephyrus/`.
Suggested first notebook: load `data/raw/agile_prices.json` + `carbon_forecast.json`
(from `make data`) and plot the price/carbon curves the optimiser consumes.
